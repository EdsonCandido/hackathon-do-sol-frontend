services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: frontend-hackathon-sol

    restart: unless-stopped

    networks:
      - proxy

networks:
  proxy:
    external: true
